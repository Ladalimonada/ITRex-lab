export default class MainController {
    constructor(model, view) {
        this.model = model
        this.view = view
    }
    // displayPage (data) {
    //     const Html = this.view.getHtml(data);
    //     document.querySelector("#root").innerHTML = Html;
    //   }
}